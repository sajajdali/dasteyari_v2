# توکن‌های ظاهری (استخراج‌شده از قالب‌ها)

این مقادیر **عیناً** در قالب‌ها استفاده شده‌اند. مقدار جدید نساز؛ اگر مقداری لازم شد که اینجا نیست، یعنی جایی از قالب منحرف شده‌ای.

## فونت
```
font-family: Vazirmatn, system-ui, sans-serif;
-webkit-font-smoothing: antialiased;
```
وزن‌های مورد استفاده: 400 (متن)، 700 (تاکید)، 800 (عنوان و دکمه)، 900 (اعداد بزرگ).
Vazirmatn باید self-host شود (`public/fonts/`) — CDN نه.

## رنگ‌ها
| نقش | مقدار |
|---|---|
| پس‌زمینه اپ | `#F5F6F8` |
| سطح کارت | `#FFFFFF` |
| متن اصلی | `#23262B` |
| متن ثانویه | `#4B5158` |
| متن خاموش | `#5A6169` · `#787F88` · `#8A9099` · `#9AA0A8` |
| placeholder | `#A9AEB6` |
| خط جداکننده | `#EDEEF1` |
| حاشیه ورودی/دکمه | `#E3E6EA` · `#E7E9EC` |
| **برند / کنش اصلی** | `#F4511E` (hover: `#C43C0E`) |
| متن تیره کنش | `#15181D` |
| موفق / سبز | `#12805A` · پس‌زمینه `#EAF7F1` · حاشیه `#C9E9DA` |
| خطر / سرخ | `#C43034` · متن `#8C3236` · حاشیه `#F5C9C9` · `#F0D5C8` |
| هشدار / کهربایی | `#8A5200` · حاشیه `#F0D49A` |
| اطلاع / آبی | `#144A9C` · حاشیه `#C9DDF8` |
| پیگیری / بنفش | `#4B45A8` · پس‌زمینه `#F5F4FF` · حاشیه `#D5D2F5` |

قاعده معنایی: بنفش = پیگیری و مهلت · کهربایی = بی‌حامی و انتظار · سرخ = توقف و رد · سبز = تایید و تکمیل · آبی = انتشار و اطلاع · برند = کنش اصلی صفحه.

## اندازه‌ها
| مورد | مقدار |
|---|---|
| شعاع کارت | `14–16px` |
| شعاع دکمه و ورودی | `11–13px` |
| شعاع برچسب (chip) | `20px` |
| ارتفاع دکمه اصلی | `44px` (فرعی `40–42px`، دکمه پرداخت موبایل `min-height:54px`) |
| ارتفاع ورودی و select | `44–46px` |
| حاشیه دکمه دارای border | `1.5px` |
| حاشیه کارت | `1px` |
| gap ردیف دکمه‌ها | `8px` |
| gap ستون کارت | `8–12px` |
| padding ورودی | `0 13px` |

## تایپوگرافی
| کاربرد | اندازه / وزن / line-height |
|---|---|
| عنوان صفحه | `19–22px` / 800 / 1.6 |
| عنوان کارت | `15–16px` / 800 / 1.7 |
| متن ردیف | `12.5–13px` / 400–700 / 1.9–2.1 |
| توضیح خاموش | `11.5–12px` / 400 / 1.8–1.9 |
| برچسب | `11.5px` / 800 |
| دکمه | `12–12.5px` / 800 |

`text-wrap: pretty` روی پاراگراف‌های توضیحی. اعداد فارسی با helper `faDigits()`.

## قواعد چیدمان
- RTL سراسری (`dir="rtl"`)، ولی اعداد و کد پرونده LTR-safe.
- گروه‌های دکمه/برچسب همیشه `display:flex; gap` — نه margin تک‌تک.
- در ستون flex عمودی: `width` برای عرض، `min-height` برای ارتفاع؛ هرگز `flex-basis` روی محور اصلی.
- هیچ hit target زیر ۴۴px.
- تصاویر: `assets/case-*.png` پرونده · `fam-*.png` خانواده · `news-*.png` اخبار · `story-*.png` روایت · `sup-*.png` حامی · `logo.png`/`logo-t.png` برند.
